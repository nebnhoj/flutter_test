import 'package:flutter/material.dart';

class Application {
  static String title = 'Application';
  static ThemeData theme =   new ThemeData(
          primaryColor:  Colors.teal,
          primaryColorDark: Colors.indigo,
          accentColor: Colors.blueGrey,
        );
}
